const db = require("../models");
const Proceso_Creativo = db.proceso_creativo;

// Create and Save a new Proceso_Creativo
exports.create = (req, res) => {
  
};

// Retrieve all Proceso_Creativo from the database.
exports.findAll = (req, res) => {
  
};

// Find a single Proceso_Creativo with an id
exports.findOne = (req, res) => {
  
};

// Update a Proceso_Creativo by the id in the request
exports.update = (req, res) => {
  
};